Queue队列:
1.PriorityQueue是具有优先级的队列:默认情况下是自然排序，也可以实现一个比较器，不是线程安全的
2.ArrayBlockingQueue:一个由数组支持的队列，采取先进先出的策略，记住的是这个是有固定容量的，当队列满的时候不能
再向其中加入元素，会发生阻塞现象，当队列空的时候也不能从其中获取元素也会导致类似的阻塞。
3.ConcurrentLinkedQueue:是一个基于链表的无界的线程安全队列，采用先进先出的策略。
4.LinkedBlockingQueue:一个基于链表的队列。
5.PriorityBlockingQueue:一个无界阻塞队列，与PriorityQueue一样按照自然排序的方式，虽然是无界的，但是当资源耗尽的时候
再执行add操作的话会抛出异常，不允许使用null元素也不允许放入不可比较的对象。